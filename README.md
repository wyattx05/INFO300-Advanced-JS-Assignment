# INFO300-Adcanced-JS-Assignment
## 4 JavaScript Programs showcasing my skills

### PrimeGaps.js
Detects prime gaps between 0 and a positive number

### Fibonacci Filter
Take input number, generates Fibonacci sequence, filters out only odd numbers

### LetterFrequency
Tells you how often a letter is repeated in a string and tells you the first letter that is repeated

### CodeDebugger
Fixed a bugged script that was supposed to tell you how many even numbers are between two numbers. There were two simple problems:
1) the 8th line was i/2 which just divides the variable when it needed to be 1 % 2 which shows the remainder  when divided which is needed becuase then we can find out how many even numbers there are
2) for line 9, the script wrote it as =+ when it needs to be += to add it to the variable